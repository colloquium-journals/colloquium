{
  "name": "@colloquium/bots",
  "version": "0.1.0",
  "private": true,
  "main": "dist/bots/src/index.js",
  "types": "dist/bots/src/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@colloquium/types": "*",
    "@colloquium/database": "*"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "typescript": "^5.2.2",
    "@colloquium/eslint-config": "*"
  }
}