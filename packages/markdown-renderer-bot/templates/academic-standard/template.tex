\documentclass[12pt,letterpaper]{article}

% Packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{times}
\usepackage[margin=1in]{geometry}
\usepackage{setspace}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{url}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{abstract}
\usepackage{xcolor}

% Define colors
\definecolor{accentblue}{HTML}{2c5aa0}
\definecolor{orcidgreen}{HTML}{16a34a}

% Hyperref setup
\hypersetup{
    colorlinks=true,
    linkcolor=accentblue,
    urlcolor=accentblue,
    citecolor=accentblue
}

% Page setup
\onehalfspacing
\pagestyle{fancy}
\fancyhf{}
\rhead{\thepage}
\lhead{$if(title)$$title$$endif$}
\renewcommand{\headrulewidth}{0pt}

% ORCID command
\newcommand{\orcid}[1]{\textcolor{orcidgreen}{\small ORCID: \href{https://orcid.org/#1}{#1}}}

% Title formatting
\title{$title$}

% Author handling - structured or simple
$if(authorList)$
\author{%
$for(authorList)$
$it.name$$if(it.isCorresponding)$\textsuperscript{*}$endif$%
$if(it.affiliation)$\\{\small\textit{$it.affiliation$}}$endif$%
$if(it.orcid)$\\{\orcid{$it.orcid$}}$endif$%
$sep$\\[1em]
$endfor$
$if(correspondingAuthor)$
\\[1em]{\small\textit{\textsuperscript{*}Corresponding author$if(correspondingAuthor.email)$: \href{mailto:$correspondingAuthor.email$}{$correspondingAuthor.email$}$endif$}}
$endif$
}
$else$
$if(authors)$
\author{$authors$}
$endif$
$endif$

% Date handling
\date{%
$if(submittedDate)$Submitted: $submittedDate$$endif$%
$if(acceptedDate)$$if(submittedDate)$ \textbullet\ $endif$Accepted: $acceptedDate$$endif$%
$if(publishedDate)$$if(submittedDate)$$if(acceptedDate)$ \textbullet\ $else$ \textbullet\ $endif$$endif$$if(acceptedDate)$ \textbullet\ $endif$Published: $publishedDate$$endif$%
$if(submittedDate)$$else$$if(acceptedDate)$$else$$if(publishedDate)$$else$\today$endif$$endif$$endif$%
}

% Abstract environment
$if(abstract)$
\renewcommand{\abstractname}{Abstract}
\abslabeldelim{\quad}
$endif$

\begin{document}

% Title page
\maketitle

% DOI
$if(doi)$
\begin{center}
\url{https://doi.org/$doi$}
\end{center}
$endif$

% Keywords
$if(keywords)$
\begin{center}
\textbf{Keywords:} $keywords$
\end{center}
\vspace{1em}
$endif$

% Abstract
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$

% Main content
$body$

\end{document}
