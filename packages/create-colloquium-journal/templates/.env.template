# Colloquium Journal Instance Configuration
# Generated on {{CREATED_AT}}

# Journal Configuration
JOURNAL_NAME={{JOURNAL_NAME}}
JOURNAL_SLUG={{JOURNAL_SLUG}}
JOURNAL_DESCRIPTION={{JOURNAL_DESCRIPTION}}
{{#if JOURNAL_DOMAIN}}JOURNAL_DOMAIN={{JOURNAL_DOMAIN}}{{/if}}

# Database Configuration
DATABASE_URL=postgresql://postgres:{{DB_PASSWORD}}@postgres:5432/{{DB_NAME}}
DB_PASSWORD={{DB_PASSWORD}}
DB_NAME={{DB_NAME}}

# Redis Configuration
REDIS_URL=redis://redis:6379

# Security Configuration
JWT_SECRET={{JWT_SECRET}}
MAGIC_LINK_SECRET={{MAGIC_LINK_SECRET}}

# Admin Configuration
ADMIN_EMAIL={{ADMIN_EMAIL}}
ADMIN_NAME={{ADMIN_NAME}}

# Application Configuration
NODE_ENV=production
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_JOURNAL_NAME={{JOURNAL_NAME}}

# Instance Configuration
INSTANCE_ID={{INSTANCE_ID}}
CREATED_AT={{CREATED_AT}}

# Bot Configuration
SELECTED_BOTS={{SELECTED_BOTS}}

# File Upload Configuration
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=10485760

# Email Configuration (Configure for production)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-password
# SMTP_FROM=noreply@{{JOURNAL_DOMAIN}}

# Optional: External Services
# ORCID_CLIENT_ID=your-orcid-client-id
# ORCID_CLIENT_SECRET=your-orcid-client-secret

# Optional: Analytics
# GOOGLE_ANALYTICS_ID=GA-XXXXXXXXX